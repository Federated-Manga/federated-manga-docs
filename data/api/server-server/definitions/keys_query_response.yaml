type: object
description: Server keys
example: {
  "server_keys": [{
    $ref: "../examples/server_key_witness_signed.json"
  }]
}
properties:
  server_keys:
    type: array
    title: Server Keys
    description: The queried server's keys, signed by the witness.
    items:
      $ref: "keys.yaml"